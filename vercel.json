{
  "version": 2,
  "buildCommand": "pnpm -F @alpharena/shared build&&pnpm -F @alpharena/db -F @alpharena/game-engine -F @alpharena/settlement -F @alpharena/realtime build&&pnpm -F @alpharena/matchmaking build&&pnpm -F @alpharena/orchestrator build&&pnpm -F @alpharena/api build",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.mjs": {
      "maxDuration": 30,
      "includeFiles": "packages/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET, POST, PUT, DELETE, PATCH, OPTIONS" },
        { "key": "Access-Control-Allow-Headers", "value": "Content-Type, Authorization" },
        { "key": "Access-Control-Allow-Credentials", "value": "true" }
      ]
    }
  ]
}
