{
  "version": 2,
  "buildCommand": "pnpm --filter @alpharena/shared build && pnpm --filter @alpharena/db --filter @alpharena/game-engine --filter @alpharena/settlement --filter @alpharena/realtime build && pnpm --filter @alpharena/matchmaking build && pnpm --filter @alpharena/orchestrator build && pnpm --filter @alpharena/api build",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "packages/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET, POST, PUT, DELETE, PATCH, OPTIONS" },
        { "key": "Access-Control-Allow-Headers", "value": "Content-Type, Authorization" },
        { "key": "Access-Control-Allow-Credentials", "value": "true" }
      ]
    }
  ]
}
